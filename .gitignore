# THE FIN 블로그 발행 저장소 — 2026-08-18 신설
# ★이 저장소에는 .gitignore 가 아예 없었다. 그래서 `tools/__pycache__/*.pyc` 가 추적됐고,
#   같은 날 만든 자체역검증 테스트가 돌 때마다 새 .pyc 가 쌓이는 구조였다.
#   루트 저장소에서 같은 병을 고친 직후(89802e8) 형제 경로에서 발견했다.
__pycache__/
*.py[cod]

# 자체역검증이 임시 폴더를 못 찾았을 때 쓰는 마지막 후보(테스트가 스스로 정리하지만 방어).
tools/_selfcheck/
